"""Multi-Bank adapters — Epic 25 Block G.

Cross-agent knowledge sharing in the new CLS architecture happens at the
schema level, not the engram level: generalised patterns travel between
banks while individual episodes stay agent-local.

Modules:
- schema_promoter: copy strong agent-local schemas into the Shared Bank.
"""
